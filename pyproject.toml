[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "latencyhist"
version = "0.1.0"
description = "cli histogram for access logs"
authors = [
    {name = "Dev", email = "dev@example.com"}
]
dependencies = [
    "click>=8.0",
]

[project.scripts]
latencyhist = "latencyhist.cli:main"
